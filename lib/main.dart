import 'package:flutter/material.dart';
import 'package:IkraTechnologies/select_action.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new MaterialApp(home: SelectActionPage());
  }
}
